# Backend URL used in production builds (npm run build + npm run preview)
# For local dev you typically don't need this because Vite proxies to http://localhost:8080.
VITE_BACKEND_URL=http://localhost:8080
